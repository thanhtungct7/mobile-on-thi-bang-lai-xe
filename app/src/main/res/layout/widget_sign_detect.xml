<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetDetectRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="10dp">

    <!-- Header -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="NHẬN DIỆN BIỂN BÁO"
        android:textSize="9sp"
        android:textColor="#99777368"
        android:layout_marginBottom="6dp"/>

    <!-- Ảnh chụp / kết quả detect (ẩn khi chưa có kết quả) -->
    <ImageView
        android:id="@+id/widgetDetectImage"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:layout_marginBottom="4dp"
        android:visibility="gone"/>

    <!-- Tên biển báo (ẩn khi chưa có kết quả) -->
    <TextView
        android:id="@+id/widgetDetectLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#FF2C2820"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginBottom="2dp"
        android:visibility="gone"/>

    <!-- Độ chính xác (ẩn khi chưa có kết quả) -->
    <TextView
        android:id="@+id/widgetDetectConf"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="#99777368"
        android:layout_marginBottom="6dp"
        android:visibility="gone"/>

    <!-- Nút mở camera (luôn hiển thị) -->
    <TextView
        android:id="@+id/widgetBtnOpenCamera"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📷  Chụp ảnh nhận diện"
        android:textSize="12sp"
        android:textColor="#FFFFFFFF"
        android:gravity="center"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:background="@drawable/widget_btn_camera"/>

</LinearLayout>
