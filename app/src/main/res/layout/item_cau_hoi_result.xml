<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp">

    <!-- Bubble circle: number shown centered, bg set dynamically -->
    <TextView
        android:id="@+id/txtCau"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="1"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/muted"
        android:background="@drawable/bubble_skip_bg" />

    <!-- Điểm liệt fire badge (top-right) -->
    <ImageView
        android:id="@+id/img_cauhoidiemliet"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_gravity="top|end"
        android:layout_margin="2dp"
        android:visibility="gone"
        app:srcCompat="@drawable/ico_fire" />

    <!-- Hidden views kept for adapter compatibility -->
    <ImageView
        android:id="@+id/img"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/txtContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/imgBB"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</FrameLayout>
